{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build with make",
            "command": "make",
            "args": [
                "-f",
                "Makefile"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": "build",
            "detail": "Uses make to build the project"
        }
    ]
}
